/**
 * Window dependencies.
 */
const {
	autoIndex,
	apiUrl,
	indexMeta,
	indexables,
	isEpio,
	lastSyncDateTime,
	lastSyncFailed,
	nonce,
	postTypes,
} = window.epDash;

export {
	autoIndex,
	apiUrl,
	indexables,
	indexMeta,
	isEpio,
	lastSyncDateTime,
	lastSyncFailed,
	nonce,
	postTypes,
};
